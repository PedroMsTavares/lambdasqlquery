---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: centos/nodejs-6-centos7
    tag: 'latest'

inputs:
- name: project-src

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    cd project-src
    tar -cvzf /tmp/release.tgz .
    ls -l /tmp
    

outputs:
- name: built-project
  path: /tmp/
